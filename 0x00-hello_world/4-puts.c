#include <stdio.h>
/**
 * main - execution starts from here
 * puts - prints the argument given with a new ine character by default
 * Return: the function returns an int 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	/**
	 * return - returns an int "0"
	 */
	return (0);
}
