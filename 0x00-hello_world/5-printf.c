#include <stdio.h>
/**
 * main - Entry point
 * Return: returns 0
 * printf - prints the argument given without a new line character
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
